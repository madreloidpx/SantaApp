describe("Save Child Request", () => {
    //Passing Tests
    test("Saved complete data to cache", () => {});
    //Failing Tests
    test("Data not failed to save in cache", () => {});
});

describe("Fetch Requests", () => {
    //Passing Tests
    test("Fetch but no ongoing requests", () => {});
    test("Fetch with requests inside cache", () => {});
    //Failing Tests
    test("Cache does not exist", () => {});
});